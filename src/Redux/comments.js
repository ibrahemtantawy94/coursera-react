import { COMMENTS } from '../shared/comments';

export const Comments = (state = Comments, action) => {
    switch (action.type) {
        // case value:
        //     return;
        //     break;
    
        default:
            return state;
        break;
    }
}